"""
src/models.py
==============

Definición de las arquitecturas de redes neuronales para el taller.

NOTA: este módulo se completará en la Fase 3 del proyecto, cuando empecemos
a trabajar con redes densas. Aquí tendremos funciones builder del estilo:

- ``build_dense_model(input_shape, hidden_units, ...)``
- ``build_lstm_model(input_shape, units, ...)``
- ``build_gru_model(input_shape, units, ...)``
- ``build_conv1d_model(input_shape, filters, kernel_size, ...)``
- ``build_mixto_model(input_shape, ...)``  # Conv1D + LSTM, etc.

Cada función devolverá un modelo de Keras compilado, listo para entrenar.

Por ahora este archivo está vacío. Lo dejamos aquí como placeholder para que
la estructura del proyecto sea consistente.
"""
